---
globs: project_summary.md, context_summary.md
name: Project Context Summary
description: A rule to consolidate and present the current state of the
  project's architectural documentation and recent session history from
  dedicated summary files.
alwaysApply: false
---

When prompted to review or retrieve the 'Project Context Summary' or 'Context History', first use the appropriate GitHub tool calls (e.g., gh_get_file_contents) to read both `ocdsoft/continue-memory/Bibleplaylist/project_summary.md` and `ocdsoft/continue-memory/Bibleplaylist/context_summary.md`. Then, synthesize a single, comprehensive report that merges the architectural overview, the current development status, and the recent conversational updates. Always present this report clearly to the user.